"""Data ingestion for the model catalog.

Run `python -m ingestion.refresh` to refresh data/models.db from live sources,
or `python -m ingestion.refresh --offline` to rebuild from bundled snapshots.
"""
